{
  "scripts": {
    "img:convert": "bash .github/scripts/convert_images_to_webp.sh",
    "build": "hugo --gc --minify -s website/ -d ../public/",
    "dev": "hugo serve -s website/ -d public/ --port 8090",
    "clean": "bash .github/scripts/clean.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230115.0",
    "typescript": "^4.9.5",
    "webp-batch-convert": "^1.0.1",
    "wrangler": "^2.9.0"
  }
}
